module world::character {
    public struct Character has key {
        id: UID,
    }
}

// Create a character and mint a owner capability to the character address

// Using the ownerCap the character shoule be able to reaname

// Must allow the admin to delete the character

// Must allow updating tribeId
